# 🏗️ Arquitectura de Metadatos Configurables

## 📊 Diagrama de Flujo

```
┌─────────────────────────────────────────────────────────────────────┐
│                         BASE DE DATOS                               │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  Tabla: parametros_config                                     │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │ clave: "ofertas_minimas_deseadas"                       │  │  │
│  │  │ valor_json: 2                                           │  │  │
│  │  │ metadata_json: {                                        │  │  │
│  │  │   "min": 1,           ← AQUÍ CAMBIAS LOS RANGOS        │  │  │
│  │  │   "max": 10,          ← AQUÍ CAMBIAS LOS RANGOS        │  │  │
│  │  │   "default": 2,       ← AQUÍ CAMBIAS EL DEFAULT        │  │  │
│  │  │   "unit": "ofertas",                                    │  │  │
│  │  │   "description": "Número mínimo de ofertas..."          │  │  │
│  │  │ }                                                       │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                ↓
                                ↓
┌─────────────────────────────────────────────────────────────────────┐
│                         BACKEND (FastAPI)                           │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  services/configuracion_service.py                            │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │ async def get_all_metadata():                           │  │  │
│  │  │     params = await ParametroConfig.all()                │  │  │
│  │  │     metadata = {}                                       │  │  │
│  │  │     for param in params:                                │  │  │
│  │  │         if param.metadata_json:                         │  │  │
│  │  │             metadata[param.clave] = param.metadata_json │  │  │
│  │  │     return metadata                                     │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  routers/admin.py                                             │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │ @router.get("/admin/configuracion")                     │  │  │
│  │  │ async def get_configuracion():                          │  │  │
│  │  │     config = await ConfiguracionService.get_all()       │  │  │
│  │  │     metadata = await ConfiguracionService               │  │  │
│  │  │                      .get_all_metadata()                │  │  │
│  │  │     return {                                            │  │  │
│  │  │         "configuracion_completa": config,               │  │  │
│  │  │         "metadata": metadata  ← ENVÍA METADATOS         │  │  │
│  │  │     }                                                   │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                ↓
                                ↓
┌─────────────────────────────────────────────────────────────────────┐
│                      FRONTEND (React + TypeScript)                  │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  services/configuracion.ts                                    │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │ export const getConfiguracion = async () => {           │  │  │
│  │  │   const response = await api.get(                       │  │  │
│  │  │     '/admin/configuracion'                              │  │  │
│  │  │   );                                                    │  │  │
│  │  │   return response.data; // Incluye metadata            │  │  │
│  │  │ };                                                      │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  hooks/useConfiguracion.ts                                    │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │ export const useConfiguracion = () => {                 │  │  │
│  │  │   const [metadata, setMetadata] = useState({});         │  │  │
│  │  │                                                         │  │  │
│  │  │   useEffect(() => {                                     │  │  │
│  │  │     const data = await getConfiguracion();              │  │  │
│  │  │     setMetadata(data.metadata); ← GUARDA METADATOS      │  │  │
│  │  │   }, []);                                               │  │  │
│  │  │                                                         │  │  │
│  │  │   return { metadata, ... };                            │  │  │
│  │  │ };                                                      │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  components/configuracion/ParametrosGeneralesForm.tsx         │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │ const { metadata } = useConfiguracion();                │  │  │
│  │  │                                                         │  │  │
│  │  │ // Genera inputs dinámicamente                         │  │  │
│  │  │ const parametros = Object.entries(metadata)             │  │  │
│  │  │   .filter(([key, meta]) => 'min' in meta)              │  │  │
│  │  │   .map(([key, meta]) => ({                             │  │  │
│  │  │     key,                                                │  │  │
│  │  │     min: meta.min,  ← USA RANGOS DE LA DB              │  │  │
│  │  │     max: meta.max,  ← USA RANGOS DE LA DB              │  │  │
│  │  │     default: meta.default,                             │  │  │
│  │  │     unit: meta.unit                                    │  │  │
│  │  │   }));                                                  │  │  │
│  │  │                                                         │  │  │
│  │  │ return (                                                │  │  │
│  │  │   {parametros.map(param => (                           │  │  │
│  │  │     <Input                                              │  │  │
│  │  │       type="number"                                     │  │  │
│  │  │       min={param.min}  ← APLICA VALIDACIÓN             │  │  │
│  │  │       max={param.max}  ← APLICA VALIDACIÓN             │  │  │
│  │  │     />                                                  │  │  │
│  │  │   ))}                                                   │  │  │
│  │  │ );                                                      │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                ↓
                                ↓
┌─────────────────────────────────────────────────────────────────────┐
│                         INTERFAZ DE USUARIO                         │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │  http://localhost:3000/configuracion                          │  │
│  │  ┌─────────────────────────────────────────────────────────┐  │  │
│  │  │  Parámetros Generales                                   │  │  │
│  │  │  ┌───────────────────────────────────────────────────┐  │  │  │
│  │  │  │ Ofertas Mínimas Deseadas                          │  │  │  │
│  │  │  │ [  2  ] ofertas                                   │  │  │  │
│  │  │  │ Min: 1 | Max: 10 ← RANGOS DESDE LA DB             │  │  │  │
│  │  │  └───────────────────────────────────────────────────┘  │  │  │
│  │  │  ┌───────────────────────────────────────────────────┐  │  │  │
│  │  │  │ Timeout de Evaluación                             │  │  │  │
│  │  │  │ [  5  ] segundos                                  │  │  │  │
│  │  │  │ Min: 1 | Max: 30 ← RANGOS DESDE LA DB             │  │  │  │
│  │  │  └───────────────────────────────────────────────────┘  │  │  │
│  │  └─────────────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
```

## 🎯 Puntos Clave

### 1. **Fuente de Verdad: Base de Datos**
- Los rangos se almacenan en `parametros_config.metadata_json`
- Cambias los valores directamente en la DB
- No necesitas tocar código

### 2. **Backend: Proveedor de Metadatos**
- Lee los metadatos de la DB
- Los incluye en la respuesta del endpoint
- Método: `ConfiguracionService.get_all_metadata()`

### 3. **Frontend: Consumidor Dinámico**
- Recibe los metadatos del backend
- Genera los inputs automáticamente
- Aplica las validaciones dinámicamente

### 4. **UI: Reflejo Automático**
- Los usuarios ven los rangos actualizados
- Sin necesidad de redesplegar
- Cambios inmediatos

## 🔄 Ejemplo de Cambio Completo

### Paso 1: Cambiar en la DB
```sql
UPDATE parametros_config
SET metadata_json = jsonb_set(metadata_json, '{max}', '20')
WHERE clave = 'ofertas_minimas_deseadas';
```

### Paso 2: Backend responde automáticamente
```json
{
  "metadata": {
    "ofertas_minimas_deseadas": {
      "min": 1,
      "max": 20,  ← NUEVO VALOR
      "default": 2,
      "unit": "ofertas"
    }
  }
}
```

### Paso 3: Frontend se actualiza automáticamente
```tsx
<Input
  type="number"
  min={1}
  max={20}  ← NUEVO RANGO APLICADO
  defaultValue={2}
/>
```

### Paso 4: Usuario ve el cambio
- Refresca la página
- El input ahora acepta hasta 20
- ✅ Sin redesplegar nada

## 📁 Archivos Modificados

### Backend:
1. `services/core-api/models/analytics.py` - Modelo con `metadata_json`
2. `services/core-api/services/configuracion_service.py` - Métodos para metadatos
3. `services/core-api/routers/admin.py` - Endpoint que envía metadatos
4. `scripts/add_metadata_to_parametros_config.sql` - Migración inicial

### Frontend:
1. `frontend/admin/src/types/configuracion.ts` - Tipos con metadata
2. `frontend/admin/src/services/configuracion.ts` - Servicio actualizado
3. `frontend/admin/src/hooks/useConfiguracion.ts` - Hook con metadata
4. `frontend/admin/src/components/configuracion/ParametrosGeneralesForm.tsx` - Componente dinámico

## ✅ Beneficios

1. **Centralización:** Un solo lugar para todas las validaciones
2. **Sincronización:** Frontend y backend siempre alineados
3. **Flexibilidad:** Cambios sin redespliegue
4. **Auditoría:** Todos los cambios registrados
5. **Escalabilidad:** Fácil agregar nuevos parámetros
